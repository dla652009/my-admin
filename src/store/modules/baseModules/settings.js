import defaultSettings from "@/settings";

const {
    title,
    sideBarState
} = defaultSettings;

const state = {
    title,
    sideBarState

};

const mutations = {

};
const actions = {

};

export default {
    namespaced: true,
    state,
    mutations,
    actions
};
